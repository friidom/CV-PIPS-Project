# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# macOS
.DS_Store

# Contest footage is never redistributed: keep the originals and any proxies of
# them out of git, but DO ship the small rendered clips the website serves.
*.mp4
*.MP4
*.avi
*.mov
*.mkv
/samples/
/data/
/cache/
/runs/
!web/public/media/**/*.mp4
!web/public/media/**/*.jpg

# Model weights other than the TorchScript graphs committed in weights/
*.pt
*.pth
*.onnx
*.engine

# Harness output (predictions_samples.json IS committed — the task asks for it)
/predictions.json

# Deployment secrets stay on the server (Cloudflare Tunnel credentials, env files)
.cloudflared/
cert.pem
.env
.env.*
# start.sh's tunnel binary and Python environment, when they sit in the checkout
/cloudflared
/.venv-wiut

# Website
web/node_modules/
web/dist/
web/.vite/
server/.jobs/
server.log
server.pid
cloudflared.log
